# Timetide - Travel Diary Application
# Python Dependencies

# FastAPI framework
fastapi==0.104.1
# Use a modern uvicorn compatible with Python 3.13 and avoid missing install issues
uvicorn[standard]>=0.29.0

# Database
sqlalchemy==2.0.23
psycopg2-binary==2.9.9  # PostgreSQL adapter
alembic==1.12.1  # Database migrations

# Authentication & Security
PyJWT==2.8.1  # JWT tokens (pure Python, no Rust)
passlib[argon2]>=1.7.4  # Password hashing with argon2 (pure Python)
python-multipart==0.0.6  # Form data parsing

# Templates
jinja2==3.1.2

# Validation (pin to version with prebuilt wheels for Python 3.13)
pydantic==2.8.2
pydantic[email]==2.8.2

# Environment variables
python-dotenv==1.0.0

# Testing (optional)
pytest==7.4.3
httpx==0.25.1  # For testing async endpoints

# Development tools (optional)
black==23.11.0  # Code formatting
flake8==6.1.0  # Linting
